export enum MessageTypeEnum {
  text = "text",
  sticker = "sticker",
  image = "image",
}

export enum RoleEnum {
  admin = "admin",
  user = "user",
}

export enum ChatTypeEnum {
  personal = "personal",
  group = "group",
}
